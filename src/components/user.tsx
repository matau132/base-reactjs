import * as React from 'react';
import { IndexedObject } from '../utils/type';

const UserPage: React.FC<IndexedObject> = () => {
  return (
    <div className="Article">
      <div>
        <h1>User Page</h1>
      </div>
    </div>
  );
};

export default UserPage;
