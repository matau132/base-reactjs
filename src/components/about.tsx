import * as React from 'react';
import { IndexedObject } from '../utils/type';

const AboutPage: React.FC<IndexedObject> = () => {
  return (
    <main>
      <h1>About page</h1>
    </main>
  );
};

export default AboutPage;
